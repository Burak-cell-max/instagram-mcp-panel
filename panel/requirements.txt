# Local publishing panel — install on top of the instagram-mcp package:
#   pip install -e .
#   pip install -r panel/requirements.txt
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
